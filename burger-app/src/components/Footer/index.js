import styled from "styled-components";

function Footer() {
    return (
        <FooterStyled>

        </FooterStyled>
    )
}

const FooterStyled = styled.div({
    width: '100%',
    height: '6%',
    background: 'black',
});

export default Footer;